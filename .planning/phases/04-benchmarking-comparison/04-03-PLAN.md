---
wave: 3
depends_on: ["04-02-PLAN"]
files_modified:
  - frontend/src/components/ReturnsMatrix.tsx
  - frontend/src/App.tsx
  - CHANGELOG.md
autonomous: true
objective: "Implement side-by-side strategy comparison and final UI/UX polish for Phase 4."
must_haves:
  - "Users can compare metrics of multiple simulations side-by-side in a table."
  - "Chart legends are clear and reflect both strategy and benchmark."
  - "All Phase 4 requirements are verified as complete."
---

# Plan 04-03: Advanced Comparison & Final Polish

## Objective

This final plan for Phase 4 focuses on improving the comparative analysis capabilities and polishing the user interface. We will enhance the "Returns Matrix" or create a new "Comparison Table" where users can see how their selected strategy stacks up against benchmarks and other strategies simultaneously.

## Context

Phase 4 goal is "Benchmarking & Comparative Dashboard". We have the backend metrics and the basic chart overlay. Now we need to make the comparison explicit and easy to digest for the user.

## Tasks

<task id="04-03-01">
<name>Multi-Strategy Comparison Table</name>
<files>
- frontend/src/components/ReturnsMatrix.tsx
- frontend/src/App.tsx
</files>
<action>
1. Create a "Comparison Table" component (or enhance the ReturnsMatrix) that displays key metrics (Total Return, CAGR, Sharpe, Sortino, Max Drawdown) for the active strategy vs. the active benchmark.
2. (Optional) Allow selecting multiple strategies for a comparison view.
</action>
<verify>
Ensure the table clearly shows the difference in metrics between the strategy and the benchmark.
</verify>
<done>[ ]</done>
</task>

<task id="04-03-02">
<name>UI/UX Polish</name>
<files>
- frontend/src/components/StrategyChart.tsx
- frontend/src/App.tsx
</files>
<action>
1. Enhance the chart legend to show labels for both the strategy and the benchmark series.
2. Improve tooltips on the chart to display values for both series at a given date.
3. Add explanatory tooltips or a "Help" section defining Sharpe/Sortino for casual investors.
</action>
<verify>
Verify that the chart is easy to read and all metrics are well-explained.
</verify>
<done>[ ]</done>
</task>

<task id="04-03-03">
<name>Final Phase 4 Verification</name>
<files>
- .planning/phases/04-benchmarking-comparison/04-VERIFICATION.md
</files>
<action>
1. Create a verification document for Phase 4.
2. Run manual tests ensuring all requirements (BENCH-01, BENCH-02, BENCH-03) are met.
3. Update CHANGELOG.md with Phase 4 accomplishments and learnings.
</action>
<verify>
All Phase 4 requirements are checked off in REQUIREMENTS.md and ROADMAP.md.
</verify>
<done>[ ]</done>
</task>

## Must-Haves

After all tasks complete, the following must be true:

- [ ] Side-by-side metric comparison is visible in the UI.
- [ ] Chart legends and tooltips support multi-series data.
- [ ] Benchmarks can be toggled effortlessly.
- [ ] Final project status updated to reflect completion of v1.0 milestone.
