---
wave: 3
depends_on: [03-02-PLAN.md]
files_modified: [frontend/src/components/StrategyChart.tsx, frontend/src/components/ReturnsMatrix.tsx, frontend/src/components/StatsGrid.tsx, frontend/src/hooks/useSimulationData.ts]
autonomous: true
---

# Plan: Visualization Components & Integration

Implement the charting, returns matrix, and data fetching logic to complete the dashboard.

## Context
With the layout in place, the core value of Phase 3 is the interactive data visualization. We'll use `Lightweight Charts` for equity curves and a custom CSS grid for the "Hedge Fund" style returns matrix.

## Tasks
<task id="viz-01">
  <action>Implement a `useSimulationData` custom hook using TanStack Query to fetch data from the backend API.</action>
  <verify>Ensure the hook correctly fetches and caches results for a selected strategy/asset combo.</verify>
</task>

<task id="viz-02">
  <action>Create a `StrategyChart` component using `lightweight-charts` to display the Equity Curve and Drawdown.</action>
  <verify>Synchronize the crosshair between the top (Equity Curve) and bottom (Drawdown) charts.</verify>
</task>

<task id="viz-03">
  <action>Create a `ReturnsMatrix` component using a CSS Grid to display monthly returns in a heatmap style.</action>
  <verify>Verify that colors correctly transition from red (negative) to green (positive).</verify>
</task>

<task id="viz-04">
  <action>Create a `StatsGrid` summary component for key metrics (CAGR, Sharpe, Max Drawdown).</action>
  <verify>Ensure metrics match the backend simulation result output.</verify>
</task>

<task id="viz-05">
  <action>Wire everything together in `App.tsx` and implement the "Run Sync" empty state CTA.</action>
  <verify>Confirm the dashboard is fully interactive: selecting an asset/strategy updates the charts and tables.</verify>
</task>

## Must Haves
- Synchronized dual charts (Equity Curve + Max Drawdown).
- Heatmap-style monthly returns matrix.
- URL-driven state (Asset and Strategy in the path).
- Responsive layout that handles varying screen sizes.
