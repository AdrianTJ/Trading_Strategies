---
wave: 2
depends_on: [03-01-PLAN.md]
files_modified: [package.json, frontend/index.html, frontend/src/main.tsx, frontend/src/App.tsx, frontend/tailwind.config.js]
autonomous: true
---

# Plan: React Frontend Scaffold & Layout

Set up the React frontend using Vite, Tailwind CSS, and the initial layout structure.

## Context
A functional frontend is required to visualize the results. We need the directory structure, styling system (Tailwind), and the primary "Clean Light" layout (Sidebar + Main Content Area).

## Tasks
<task id="frontend-01">
  <action>Initialize a new Vite project in the `frontend/` directory (React + TypeScript).</action>
  <verify>Run `npm run dev` and ensure the default Vite app is accessible.</verify>
</task>

<task id="frontend-02">
  <action>Install and configure Tailwind CSS and its dependencies in the `frontend/` directory.</action>
  <verify>Add a Tailwind class to `App.tsx` and verify the styling is applied correctly.</verify>
</task>

<task id="frontend-03">
  <action>Create the primary dashboard layout with a left sidebar (for assets) and a main content area.</action>
  <verify>Verify the sidebar is correctly positioned and visible in the browser.</verify>
</task>

<task id="frontend-04">
  <action>Install `lightweight-charts`, `lucide-react`, and `@tanstack/react-query`.</action>
  <verify>Verify packages are added to `package.json` and basic imports work.</verify>
</task>

## Must Haves
- Vite-based React project with TypeScript support.
- Tailwind CSS integrated and working for layout.
- Basic routing structure (React Router) to handle `/dashboard/:asset/:strategy`.
