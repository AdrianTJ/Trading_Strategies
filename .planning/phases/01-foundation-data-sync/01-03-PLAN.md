---
wave: 3
depends_on: [01-02]
files_modified: [src/ingest/alignment.py, src/db/aligned_models.py]
autonomous: true
---

# Plan: 01-03 - Data Alignment & Pre-alignment

## Objective
Align heterogeneous FRED data series into a unified daily time-series table ready for simulation.

## Requirements
- **DATA-04**: Data alignment logic to merge monthly FRED series with daily price data (Forward-Fill).

## Must Haves
- `src/ingest/alignment.py` with pandas logic for upsampling and forward-filling data.
- SQLModel for `AlignedObservation` table in `src/db/aligned_models.py`.
- Alignment script to generate the pre-aligned table.

## Tasks

<task id="1">
<action>
Implement `src/ingest/alignment.py` with logic to upsample monthly CPI to daily using `.resample('D').ffill()`.
</action>
<verify>
Monthly data is correctly expanded to daily with constant values.
</verify>
</task>

<task id="2">
<action>
Implement `AlignmentEngine` class to merge all 4 series into a single wide DataFrame on the date index.
</action>
<verify>
`AlignmentEngine` produces a daily DataFrame with no missing values (all FFilled).
</verify>
</task>

<task id="3">
<action>
Implement `src/db/aligned_models.py` for storing the unified time-series.
</action>
<verify>
SQLModel correctly represents the 4 aligned series in a single table.
</verify>
</task>

<task id="4">
<action>
Create a script `scripts/align_data.py` that populates the aligned database table.
</action>
<verify>
`python scripts/align_data.py` results in a single table containing 5 years of daily aligned data.
</verify>
</task>
