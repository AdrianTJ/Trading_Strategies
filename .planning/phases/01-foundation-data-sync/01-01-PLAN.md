---
wave: 1
depends_on: []
files_modified: [src/db/models.py, src/db/session.py]
autonomous: true
---

# Plan: 01-01 - Database Infrastructure

## Objective
Set up the local SQLite database using SQLModel to store historical series metadata and observations.

## Requirements
- **DATA-03**: Local persistent storage (SQL) for all fetched historical data.

## Must Haves
- `src/db/models.py` with `Series` and `Observation` models.
- `src/db/session.py` with engine and session management.
- Script to initialize the database and run basic CRUD tests.

## Tasks

<task id="1">
<action>
Create `src/db/models.py` with `Series` and `Observation` SQLModel definitions.
</action>
<verify>
Models are correctly defined with indices on `series_id` and `date`.
</verify>
</task>

<task id="2">
<action>
Create `src/db/session.py` for SQLite engine and session creation.
</action>
<verify>
Database file path is configurable and engine connects successfully.
</verify>
</task>

<task id="3">
<action>
Create a test script `tests/test_db.py` to verify database initialization and observation storage.
</action>
<verify>
`pytest tests/test_db.py` passes.
</verify>
</task>
