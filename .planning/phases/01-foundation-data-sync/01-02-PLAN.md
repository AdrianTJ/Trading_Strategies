---
wave: 2
depends_on: [01-01]
files_modified: [src/ingest/fred_client.py, src/main.py]
autonomous: true
---

# Plan: 01-02 - FRED API Client

## Objective
Implement a client for syncing historical data from the FRED API into the local database.

## Requirements
- **DATA-01**: Automated sync with FRED API for S&P 500, Gold, and US Bonds (10-year Treasury).
- **DATA-02**: Fetch and store CPI (Consumer Price Index) for "Real Return" calculations.

## Must Haves
- `src/ingest/fred_client.py` using `fredapi` library.
- Ingestion script to fetch 5 years of data for `SP500`, `GOLDAMGBD228NLBM`, `DGS10`, and `CPIAUCSL`.
- Secure management of API key through `.env`.

## Tasks

<task id="1">
<action>
Implement `src/ingest/fred_client.py` using the `fredapi` library.
</action>
<verify>
FRED client correctly initializes with API key from `.env`.
</verify>
</task>

<task id="2">
<action>
Implement a `SyncManager` class in `src/ingest/fred_client.py` to coordinate the 5-year data sync.
</action>
<verify>
`SyncManager` fetches 5 years of data for the 4 core series IDs.
</verify>
</task>

<task id="3">
<action>
Create a script `scripts/sync_data.py` that uses the `SyncManager` and stores observations in the database.
</action>
<verify>
`python scripts/sync_data.py` successfully populates the SQLite database with 5 years of data for all 4 series.
</verify>
</task>
