---
wave: 3
depends_on: [02-02]
files_modified: [src/engine/analytics.py, scripts/run_simulations.py]
autonomous: true
---

# Plan: 02-03 - Performance Analytics & Simulation Runner

## Objective
Implement performance metrics (Sharpe, Sortino, Drawdown) and a runner to automate simulations across all assets and store results.

## Requirements
- **BENCH-03**: Calculation and display of risk metrics: Sharpe Ratio, Sortino Ratio, and Max Drawdown.

## Must Haves
- `src/engine/analytics.py` for risk metrics and performance calculations.
- `scripts/run_simulations.py` to run all strategies and save results to the database.
- Integration of CPI for inflation-adjusted "Real Returns".
- Automated verification of the full simulation flow.

## Tasks

<task id="1">
<action>
Implement `src/engine/analytics.py` with Sharpe Ratio, Sortino Ratio, and Maximum Drawdown logic.
</action>
<verify>
Analytics correctly annualizes daily metrics using a risk-free proxy (10Y Treasury).
</verify>
</task>

<task id="2">
<action>
Implement `scripts/run_simulations.py` to iterate through assets and strategies, calculate results, and save them.
</action>
<verify>
`python scripts/run_simulations.py` populates `SimulationResult` and `DailyPerformance` tables.
</verify>
</task>

<task id="3">
<action>
Implement "Real Return" logic using CPI in the analytics module.
</action>
<verify>
Performance metrics include both nominal and inflation-adjusted outcomes.
</verify>
</task>

<task id="4">
<action>
Create `tests/test_analytics.py` to verify risk metrics for a known volatile series.
</action>
<verify>
`PYTHONPATH=. uv run pytest tests/test_analytics.py` passes.
</verify>
</task>
