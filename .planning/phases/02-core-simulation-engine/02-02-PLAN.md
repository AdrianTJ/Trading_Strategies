---
wave: 2
depends_on: [02-01]
files_modified: [src/engine/strategies.py, src/engine/simulator.py]
autonomous: true
---

# Plan: 02-02 - Core Strategy Logic

## Objective
Implement the vectorized simulation engine and the three core investment strategies (DCA, Lump Sum, Dip Buying).

## Requirements
- **STRAT-01**: Vectorized simulation engine for Weekly and Monthly DCA.
- **STRAT-02**: Simulator for Lump Sum strategy.
- **STRAT-03**: Simulator for "Buy the Dip" (5%) strategy.
- **STRAT-04**: Transaction cost simulator (fees/commissions).

## Must Haves
- `src/engine/strategies.py` with vectorized strategy logic using Pandas.
- `src/engine/simulator.py` to orchestrate simulations across different assets and strategies.
- Handling of transaction costs ($1.00 fee) and slippage (0.05%).
- Next-day open execution logic.

## Tasks

<task id="1">
<action>
Implement `src/engine/strategies.py` with vectorized functions for Lump Sum, DCA (Weekly/Monthly), and 5% Dip signals.
</action>
<verify>
Strategy signals are correctly generated as a boolean mask on the daily DataFrame.
</verify>
</task>

<task id="2">
<action>
Implement the `Simulator` class in `src/engine/simulator.py` to calculate positions and cash balances based on signals.
</action>
<verify>
Simulator correctly accounts for fixed fees and 0.05% slippage on each trade.
</verify>
</task>

<task id="3">
<action>
Enforce strict point-in-time logic by shifting signal execution by +1 day.
</action>
<verify>
No look-ahead bias is detected; trades occur at the price following the signal.
</verify>
</task>

<task id="4">
<action>
Create `tests/test_strategies.py` to verify returns for a simple known price series.
</action>
<verify>
`PYTHONPATH=. uv run pytest tests/test_strategies.py` passes with expected ROI for all 3 strategies.
</verify>
</task>
