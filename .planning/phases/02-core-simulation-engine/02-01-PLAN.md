---
wave: 1
depends_on: []
files_modified: [src/db/results_models.py, src/db/session.py]
autonomous: true
---

# Plan: 02-01 - Simulation Results Persistence

## Objective
Set up the database tables to store strategy simulation results, including aggregate metrics and daily performance logs.

## Requirements
- **STRAT-04**: Store results in a persistent database table.

## Must Haves
- `src/db/results_models.py` with `SimulationResult` and `DailyPerformance` SQLModel definitions.
- Migration to update the SQLite database with the new tables.
- Verification script to test CRUD operations on results.

## Tasks

<task id="1">
<action>
Create `src/db/results_models.py` with `SimulationResult` (aggregate metrics) and `DailyPerformance` (daily logs) models.
</action>
<verify>
`SimulationResult` includes fields for Sharpe, Sortino, Max Drawdown, and Total Return.
</verify>
</task>

<task id="2">
<action>
Update `src/db/session.py` to ensure all models (including results) are initialized.
</action>
<verify>
`init_db()` correctly creates the new tables in `trading_strategies.db`.
</verify>
</task>

<task id="3">
<action>
Create a test script `tests/test_results_db.py` to verify result storage.
</action>
<verify>
`PYTHONPATH=. uv run pytest tests/test_results_db.py` passes.
</verify>
</task>
